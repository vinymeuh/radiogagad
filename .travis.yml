language: go

env:
- GO111MODULE=on

go:
- 1.12.x

env:
  matrix:
    - TARGET="build"
    - TARGET="buildarm7"
    - TARGET="test"
script: make $TARGET

after_success:
  - bash <(curl -s https://codecov.io/bash)
